# This machine has core.autocrlf=true globally, which would check these files
# out with CRLF and produce whole-file diffs against the LF stored in the repo.
# The site is built on Linux (locally and in Actions), so pin everything to LF.
* text=auto eol=lf

*.jpg binary
*.png binary
*.svg binary
*.pdf binary
