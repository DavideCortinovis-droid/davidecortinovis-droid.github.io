source "https://rubygems.org"

# Matches what actions/jekyll-build-pages runs in CI, so local previews and the
# deployed site agree. The theme uses no plugins of its own.
gem "github-pages", group: :jekyll_plugins
